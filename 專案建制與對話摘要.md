# 祕魯 55 周年｜專案建制與對話摘要

> 本檔為 **2026-04-09 前後** 建制本專案時的約定與對話重點整理，供換 Cursor 對話窗、換電腦或交接時快速接軌。  
> **日常現況**仍以 `專案最新備註.md` 為準（離開／回來流程見該檔與 `.cursor/rules/peru-55-continue.mdc`）。

---

## 一、專案定位

| 項目 | 說明 |
|------|------|
| 本機路徑 | `C:\Users\user\Documents\祕魯55周年` |
| 用途 | 官網 **「行程特色」** 自訂 HTML（與 **夏古號／官網正式** **同一套後台、同一欄位**） |
| 與夏古號關係 | 工作流程、CMS 限制（`<style>` 上限、截斷風險等）**沿用夏古號實測**；**獨立 Git repo**，class／id 前綴改為 **`pe55-`**，避免與夏古號 `tc-` 混淆 |

---

## 二、GitHub 與同步

| 項目 | 內容 |
|------|------|
| 帳號 | `sammyzomb`（與 `xia-gu-hao` 同一帳號） |
| 新 repo 名稱 | `peru-55-anniversary` |
| 遠端網址 | https://github.com/sammyzomb/peru-55-anniversary |
| 分支 | `main`，已設定 `origin` 並完成首次推送 |
| 換電腦 | `git clone` 上列網址後進入專案目錄工作；開工前 `git pull` |

**對話中強調**：提交時宜 **精準 `git add` 檔案**，避免 **`git add -A`** 把未整理大圖、`.exe`、`.winmd` 等推上遠端（`.gitignore` 已列常見排除項）。

---

## 三、檔案與制度（與夏古號同構）

| 檔案／目錄 | 角色 |
|------------|------|
| `貼官網用/行程特色_貼上版.html` | **唯一上線**：全選複製 → 後台「行程特色」 |
| `貼官網用/行程特色_開發版.html` | 日常編輯（註解），定稿後同步貼上版 |
| `貼官網用/行程特色_安全版_RULES.md` | 後台禁忌、**基準標籤**、還原指令、口頭約定「上一版正常」 |
| `專案最新備註.md` | 版本對照表、已完成／要注意／待辦、最後更新日 |
| `.cursor/rules/peru-55-continue.mdc` | Cursor：「我回來了」「我要離開了」「還原正常版」等觸發行為 |
| `scripts/` | 可選：每小時提醒（工作名稱 **`Peru55-BaselineHourlyReminder`**，與夏古號排程可分開並存） |

---

## 四、HTML／CMS 技術約定（摘要）

- 根節點：**`#pe55-cms-safe-root`**；內容區建議 **`#pe55-root`**。
- 新增 **id／class** 一律 **`pe55-` 前綴**。
- 貼上版目標：**1 個 `<style>`**、**1 個 `<script>`**（與夏古號策略一致）。
- CMS：**`<style>` 總數上限 8，實務 ≤7**；過長 HTML 仍可能 **截斷** → 每日行程／地圖不見時先懷疑截斷或過濾。
- 互動 class 只加在 **`#pe55-cms-safe-root` 內**，勿對 `<html>` 加 class。

---

## 五、基準版（Git 標籤）— 尚未做

- 建議標籤格式：**`pe55-feature-baseline-YYYYMMDDhhmm`**（例：`pe55-feature-baseline-202604151030`）。
- **時機**：官網貼上後，確認行程特色、**每日行程**、**地圖**、頁尾皆正常，再 `commit` → `tag` → `push` 標籤。
- 然後更新：**`行程特色_安全版_RULES.md` 第一節**、`專案最新備註.md`、**`.cursor/rules/peru-55-continue.mdc`** 內預設還原標籤名稱。

（截至摘要撰寫時：**第一個基準標籤尚未建立**，貼上版仍為占位內容。）

---

## 六、對話中其他重點

1. **同一 GitHub 帳號、新專案**：已新建 `peru-55-anniversary`，與 `xia-gu-hao` 並列、互不影響。  
2. **Cursor Chat 無法整段「搬」到另一資料夾**：要聊祕魯案請 **File → Open Folder** 選 `祕魯55周年`；脈絡靠 **本 repo 的 md／RULES** 與必要時手動貼上對話摘要。  
3. **本檔**即為該次對話之建制摘要，可與 `專案最新備註.md` 並讀。

---

*摘要建立：2026-04-09*
